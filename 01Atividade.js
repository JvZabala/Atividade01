// 1. Escreva um programa que recebe um número inteiro e verifica se ele é par ou ímpar
// utilizando uma estrutura de controle if.

let number = 15
if (number % 2 == 0){ // Condição:Verifica se o resto da divisão do número por 2 é igual a 0, assim provando se o número é par ou impar.
    console.log('PAR')}
else {console.log('IMPAR')}
